# 任务01 - 项目结构与基础约定

## 目标
- 按项目结构规范搭建前端目录与模块边界，保证 feature 化组织。

## 主要工作
- 补齐 `src/pages`、`src/layouts`、`src/services`、`src/types`、`src/utils` 等目录。
- 建立 `src/features` 下的模块基础结构与 public API 约定。
- 建立路由入口与页面空壳，页面仅负责布局与路由。

## 产出
- 规范化目录结构与基础路由骨架。
- 每个 feature 的 `index.ts` public API 模板。
